import { FilterCustomPipe } from './filter-custom.pipe';

describe('FilterCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
